export type record = {
  displayName?: string;
  goalUserId?: string;
  entityType?: string;
  identityCard?: string;
  personalNumber?: string;
  firstName?: string;
  source?: string;
  userId?: string;
};
